extends: stickerchat_v6_minimal_core.yaml
name: stickerchat_v6_expr_only
description: "StickerChat minimal-core ablation: MM-BERT + expression branch only."
pl_root_dir: logs/stickerchat_factorized_v6_expr_only

lambda_expr: 0.3
lambda_style_proto: 0.0
