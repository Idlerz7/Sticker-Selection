# Phase 4: query-conditioned scaling on injected tokens (query_gate).
extends: _base.yaml
name: v2_05_expr_style_query_gate
description: "expr+style tokens modulated by dialogue query embedding (sigmoid gate per dim)."
struct_token_mode: expr_style
token_condition_mode: query_gate
pl_root_dir: logs/structured_tokens_v2_05_expr_style_qgate
